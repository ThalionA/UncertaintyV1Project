# nn_decoder Audit — 2026-08-25

**Status (updated 2026-08-25, end of session): bundles 1, 3, 4 and the dedup pass
are APPLIED.** Six commits on `main`, tests green (657 passed; the 3 failures are
pre-existing and verified identical on a stashed clean tree). Per-item status is
marked inline below. Still open: **B1** (the shuffle-null asymmetry — deliberately
left for a decision, see its entry), **B7**, the remaining **B10** items, **D6/D7**,
all of **P2–P4** and **S1–S5**.

What shipped:
| Commit | Contents |
|---|---|
| `250e5e6` | Bundle 4 — dead code + path quick wins (X1, X3, X4, P1) |
| `f5e6965` | Bundle 1 — bug pass (B2–B6, B9) |
| `6a1b65f` | figsave hardening (B8) + metrics home (D1) + palette (D3) |
| `adb5eec` | Bundle 3 — 97 savefig sites onto the sink, + 38 layout regressions fixed |
| `13072aa` | dedup — shared `kl_per_trial`, lazy scorer import; D4 refuted |
| `87bb86f` | D2 — one cell-slug implementation, producer and consumer |
| `40d87f0` | B10 selections — evar-knob exclusivity, preset deep-copy, clear model_type error |

**Two report items were WRONG and are corrected inline: X2** (the eight "dead"
plotting helpers are live intra-module callees) and **D4** (the local `rcParams`
blocks are not duplicates of `ps.apply()` — measured). Both are recorded in
`CONVENTIONS.md` so the next audit skips them.

**One finding upgraded to a GOTCHA:** the diagnostics' KL and the training KL use
different eps conventions and disagree by ~2x on peaked posteriors — see D1.

Scope: `nn_decoder/` (root, `diagnostics/`, `audit/`, `training/`; `legacy/` grep-only).
Method: four parallel sweeps (import graph/dead code, plotting duplication, data-loading
duplication, core-module bug hunt) + lead-auditor verification. **Every item marked
`[verified]` was re-checked against the source by the lead auditor; `[agent]` items were
reported by a sweep and spot-checked only.** The dead-code sweep under-reported (it missed
`run_flatevar_v1.py` and the nested `nn_decoder/nn_decoder/` tree, both found by hand) —
treat its "no dead code" conclusion as weak evidence, not proof.

Prior art, not re-litigated here:
- `audit/REPORT_2026-05-19.md` + `CONVENTIONS.md` — layout, `paths.py`, `--out-dir` rule.
  Still-open items carried forward below as P2 (=3.2), P3 (=3.5/3.6), P4 (=4.1), S3 (=2.6).
- `documents/AUDIT_2026-07.md` — the July code audit. Its M1–M6/C1/C2/C4–C7 fixes are in.
  Its **deliberately-unfixed** list still stands and is NOT re-opened here: single-permutation
  shuffle null (C3), training-side CE `log(softmax+eps)` saturation, `shape_lambda`
  renormalisation, 2-D hpsweep grids without a consumer. Its §4 consolidation plan
  (`run_layout.py` + widening `decoder_metrics.py`) **was never executed** — that is now
  items D1/D2 below, with post-July growth folded in.

Current size: ~78k lines of Python across ~190 non-legacy files (94 in `diagnostics/`).

---

## 1. Bugs (B)

### B1. LATENT, NOT LIVE (census 2026-08-26) — Shuffle arms would see REAL validation targets when `val_frac > 0` — HIGH-if-triggered
- **Files**: `run_experiment.py:549-552` (Y_val built from real `target_distr_`), `:565`
  (only `Y_train_in` permuted), `:637-661` (one `training_params` dict, containing the real
  `X_val_in`/`Y_val_in`, passed verbatim to all four arms incl. both shuffle arms).
- **Problem**: with `val_frac>0`, the shuffle arms are early-stopped and (since the
  2026-07-16 fix, `restart_selection='val'`) restart-selected on **unshuffled** validation
  targets — the null model is selected on real-data signal. And when `val_frac=0,
  patience>0`, `fit_model` carves val from the already-shuffled tensors
  (`nn_classifier.py:546-554`), so val IS shuffled — two different shuffle nulls depending
  on val config. Any shuffle-normalised ratio compared across runs with different val
  configs mixes nulls. Effect size on published ratios **unmeasured**; which runs are
  exposed (val_frac>0 + shuffle-normalised claims) needs a config census before any number
  is re-stated.
- **CENSUS RESULT (2026-08-26): zero exposed cells. No published number is affected.**
  Scanned all 1059 on-disk `config.yaml` files plus the 38 pre-refactor root `.mat`
  configs (1097 fit configurations): **`val_frac` is 0.0 in every single one.** So every
  run took one of the two safe paths — 804 cells at `val_frac=0, patience>0`, where
  `fit_model` carves its val slice from the ALREADY-SHUFFLED tensors (symmetric null,
  correct), and 255 cells with no val slice at all. The `…_vf0p1/vf0p3` cells the July
  audit mentioned vary **`val_fraction`** (the internal carve), not `val_frac` — the
  two-key confusion is itself worth remembering.
- **This reverses the earlier fix-vs-document trade-off.** The original worry was that
  fixing B1 would break comparability with existing runs. It cannot: with `val_frac=0`,
  `X_val_in`/`Y_val_in` are `None`, so shuffling `Y_val_in` is a no-op and the fix is
  bit-for-bit identical for all 1097 existing configurations. The fix is therefore free,
  and it disarms the trap before the first `val_frac>0` run — which is a plausible next
  step, since `val_frac` is the documented train-vs-val diagnostic switch.
- **Solution**: permute `Y_val_in` with the same trial-permutation semantics for the two
  shuffle arms (or refuse `val_frac>0` on the shuffle arms outright); add the missing test;
  record the two-null distinction in GOTCHAS.
- **Confidence**: high (construct, verified in source); **measured (impact): zero on disk**.

### B2. ✅ DONE (f5e6965) — `Config.random_state` is a dead knob [verified] — HIGH
- **Files**: `training/config.py:233` (field), `to_legacy_dict` (absent),
  `run_experiment.py:463,467,482` (hardcodes `random_state=42`).
- **Problem**: `Config(random_state=7)` silently yields the same split as 42 — a
  split-robustness sweep would report fake stability. No existing run is wrong (default==42),
  but it's the exact `to_legacy_dict` plumbing-gap class the repo has been burned by.
- **Solution**: thread through `to_legacy_dict` → 3 call sites; default unchanged → no-op
  for production. Extend the plumbing test (see B9).
- **Confidence**: high.

### B3. ✅ DONE (f5e6965) — `synthetic_ppc` / `synthetic_sbc` target branch crashes [verified] — HIGH
- **Files**: `run_experiment.py:412-417`; `utils.py:222,262` (generators return
  `(likelihoods, posteriors)` tuples).
- **Problem**: the tuple is assigned whole to `raw_targets`; `raw_targets.T` at `:496`
  raises `AttributeError`. The documented synthetic-target control is unusable. Loud crash,
  so no silent wrong science — a regression from when generators returned one array.
- **Solution**: `raw_targets = generated_targets[1]`; add a smoke test for both branches.
- **Confidence**: high.

### B4. ✅ DONE (f5e6965) — Silent CE fall-through in both loss dispatchers [verified] — MED
- **Files**: `nn_classifier.py:255-256` (`custom_loss_all_H`), `:431-432`
  (`fit_loss_per_trial`) — CE is the bare `else`.
- **Problem**: a typo'd/wrong-case loss string (`'kl'`, `'Brier'`) silently trains CE under
  the wrong label — the silent-ReLU footgun pattern, which the activation registry already
  fixed (`:110-113`) but the loss dispatch didn't. Config-driven runs are guarded; direct
  importers (sweep/diagnostic scripts, of which there are many) are not.
- **Solution**: explicit `'CE'` branch + `else: raise ValueError`, both sites; tiny test.
- **Confidence**: high.

### B5. ✅ DONE (f5e6965) — No guard for Wasserstein / `smooth_lambda` on the 72-bin circular support — MED
- **Files**: `training/config.py:69-70` (comment says "Do NOT pair"), `__post_init__`
  (no check); `nn_classifier.py:45-53` (linear cumsum), `:459` (no 71↔0 wrap term).
- **Problem**: an `io_hmm_pkl` config with either knob runs to completion with silently
  wrong loss geometry (mass across the circular wrap priced maximally instead of minimally).
- **Solution**: one `raise` in `__post_init__`; test.
- **Confidence**: high (guard absent); med (someone trips it).

### B6. ✅ DONE (f5e6965) — Two different default pkl paths for the same knob [verified] — MED
- **Files**: `training/config.py:75` (`data/fitted_data_and_posteriors_hmm.pkl`) vs
  `run_experiment.py:368` fallback (`data/fitted_data_and_posteriors.pkl` — the older
  non-HMM file).
- **Problem**: a hand-built legacy dict with `target_source='io_hmm_pkl'` and no explicit
  path silently trains on non-HMM marginals labelled like an HMM run.
- **Solution**: single constant (home: `paths.py`), both sites import it.
- **Confidence**: high.

### B7. Greedy barcode alignment can mis-assign trials near a dropped trial [agent] — MED
- **Files**: `io_hmm_data.py:459-472` (first-match greedy on `(ori, disp, contrast)`),
  `:488` (0.95 choice-agreement floor can't catch single-trial slips).
- **Problem**: when a dropped pkl trial and the true match share a barcode in the same
  neighbourhood, an export trial silently receives a different trial's posterior.
  Exposure ~a handful of trials/mouse; incidence unmeasured.
- **Solution**: strengthen the invariant (match on choice where defined / assert dropped
  indices are barcode-unique in a window), and add the missing tests (S2 — `io_hmm_data.py`
  currently has ZERO tests).
- **Confidence**: med.

### B8. ✅ DONE (6a1b65f) — `figsave.save_fig` cap is best-effort and its log misreports dpi — MED
- **Files**: `figsave.py:64-73` (`except Exception: dpi_fit = dpi` — cap silently dropped),
  `:89` (prints nominal `dpi`, not the `dpi_fit` actually used).
- **Problem**: matches the 2026-08-23 measured failure (6/13 PNGs at 1603–1822 px). No
  post-save pixel check exists; the log violates "measure the object".
- **Solution**: post-save `PIL`/`imagesize` check + downscale-retry; print `dpi_fit`;
  first-ever tests for `figsave` (S2).
- **Confidence**: high (construct).

### B9. ✅ DONE (f5e6965) — The plumbing regression test is stale — MED
- **Files**: `tests/test_training_config.py` (`needed` set frozen at 21 keys) vs
  `run_animal_decoder`'s current reads (16+ newer keys incl. `restart_selection`, `seed`,
  `smooth_lambda`, the `io_hmm_*` triple).
- **Problem**: the test exists to catch `to_legacy_dict` omissions and would currently miss
  the exact regression class it was written for (and did not catch B2).
- **Solution**: derive the needed-key set from what `run_animal_decoder` actually reads (or
  at minimum extend + add the converse direction: every Config field lands in the dict or is
  explicitly listed as recorded-only).
- **Confidence**: high.

### B10. PARTIAL (40d87f0: b, c, g done) — Low-severity cluster [agent]
(a) `utils.py:35,45,375` seed the **global** numpy RNG (process-wide side effect; GOTCHAS
covers the single-permutation aspect, not the side effect); (b) `flat_evar`/`shape_lambda`/
`evar_alpha` exclusivity by silent `if/elif` (`run_experiment.py:209-221`); (c) `_PRESETS`
shallow-copies `hidden_sizes` (`training/config.py:616-618`) — in-place mutation corrupts
the module-global preset; (d) train/val "total" curves omit `smooth_lambda`'s term
(`nn_classifier.py:630-676` vs `:584-586`); (e) NaN val loss defeats early stopping →
silently returns final not best weights (`:605`); (f) final-epoch weight snapshot missed
under ES (`:663-664`); (g) `get_model_probabilities` unknown model_type →
`UnboundLocalError` (`:161-169`); (h) `'temp' in key` substring routing
(`run_experiment.py:257`); (i) `time_binned_ppc.fit_pca_basis` uses condition_mean fit on
ALL trials while production is all_trials/train-only — caption mislabel risk
(`time_binned_ppc.py:234-256`); (j) `io_hmm_data._install_stubs` clobbers
`sys.modules['jax']` unconditionally.

---

## 2. Duplication (D) — drift risk

### D1. ✅ DONE (6a1b65f, 13072aa) — `decoder_metrics.py` widened; ⚠ eps finding below
- **Files**: `decoder_metrics.py` (4 functions, 2 importers). Local copies: KL ×7
  (`spat_temp_performance.py:53`, `time_binned_ppc.py:223`, `within_mouse_variants.py:39`,
  `plot_ideal_observer.py:87`, `deepsets_uncertainty.py:405`,
  `diagnostics/io_hmm_v1_scorecard.py:41`, `peaky_bins_report.py`/
  `best_cell_example_gallery.py` byte-identical pair); entropy ×11 (eps varies 1e-10 /
  1e-12 / none across copies); peakiness ×8 (July count, 2 byte-identical); mean±sem
  (the ddof drift July's M3 already fixed twice).
- **Problem**: drift risk — the July audit found three *actual* drifts in exactly these
  families. Each eps/ddof variant is a latent numerical divergence.
- **Solution**: widen `decoder_metrics.py` with `kl_rows`, `js_rows`, `entropy_rows`,
  `mean_sem` (ddof=1), `peakiness`; port the ~26 call sites; pin with tests FIRST
  (mistakes-ledger `duplication` rule: hoist to src with a test, then port).
- **Confidence**: high. ~1 session incl. tests.

### D2. ✅ PARTIAL (87bb86f) — cell-slug unified (the drifted, bug-carrying part); run-name/LOSSES constants left alone
- **Files**: `_slug`/`_cell` copies in 10+ files (e.g. `plot_weight_evolution_cell.py:75`,
  `plot_overfit_vs_width.py:78`, `within_mouse_loss_plots.py:47`, `posterior_pca_views.py:55`,
  `diagnostics/spat_temp_by_lambda.py:53`, …); run-name strings (`loss_comparison_v1` ×12+,
  `io_hmm_v3`, `prodfix_v1`, `projflat_v1`, …) in 20+ files; `LOSSES` tuples in **41** files
  (3+ variants); `MICE` in 10+.
- **Problem**: July §4's #1/#5 (cell-slug 22 sites, token codec 12 sites) — already drifted
  once (`_g` bug, fixed as M5) and re-growing with each new script family.
- **Solution**: create `run_layout.py` (run-name constants, slug builders, float↔token
  codec, LOSSES/LOSS_SLUG, MICE); port the live consumers (hpsweep + io_hmm families
  first); `hpsweep_spec` keeps its axes but imports the shared pieces. Completed-run
  scripts that are effectively frozen records can be left unported and listed as such.
- **Confidence**: high on the smell; med on how many of the 41 files are worth porting.

### D3. ✅ DONE (6a1b65f) — four conflicting `LOSS_COLOR` dicts now use `ps.color()`
- **Files**: `plot_kl_js_sweep.py:8` (PCA=red, KL=blue) vs `plot_weight_evolution_cell.py:15`
  (PCA=orange, KL=purple) — same losses, different colours, both live; + 6 more copies
  (`pca_loss_demo.py`, `plot_loss_sweep.py`, `plot_overfit_vs_width.py`,
  `posterior_pca_views.py`, `diagnostics/loss_smoothness_demo.py`, `plot_kl_js_training.py`).
- **Problem**: July §4 #7 ("spatial is orange in some figures, grey/blue in others") —
  cross-figure colour semantics are already inconsistent in the vault.
- **Solution**: delete the dicts, call `ps.color(name)`. NOTE: regenerated figures will
  change colour — the vault copies won't match until re-synced.
- **Confidence**: high. ~1 hour.

### D4. ~~Nine local `rcParams` blocks vs `ps.apply()`~~ — **REFUTED BY MEASUREMENT (2026-08-25)**
- The sweep called these duplicates of `peakiness_style.apply()`. They are not.
  Measured: `ps.apply()` sets 37 rcParams and differs from the local blocks on
  most keys they set — `font.size` 9 → 15.3, `grid.linewidth` 0.5 → 1.5,
  `axes.linewidth` 0.8 → 1.2, `axes.edgecolor` '#333' → '.15', `savefig.dpi`
  200 → 'figure'. Porting them would **restyle every figure** those scripts
  produce, which is a restyle masquerading as a refactor. The only near-pair
  (`plot_fano_factor` / `population_metrics_vs_uncertainty`) shares an identical
  rcParams dict but differs in `sns.set_context(font_scale=)`, so hoisting the
  8 lines buys a cross-module dependency and a parameter for no drift protection.
  **No action taken**; recorded in `CONVENTIONS.md` so the next audit skips it.

### D5. PARTIAL (13072aa) — import weight fixed; the rename DECLINED, see below
- **Problem**: July §4 #2 — private names as a de-facto public API; drags seaborn into
  every diagnostic import.
- **Solution**: promote a public `evaluate_run()`/`aggregate()` API (natural home:
  `decoder_metrics`, bundled with D1), keep aliases one cycle, port importers.
- **Confidence**: high.

### D6. The results-dir walk is re-implemented in 10+ diagnostics [agent, pattern spot-checked]
- **Files**: `diagnostics/performance_vs_hparams.py:100-106` and near-identical loops in
  `overfit_vs_capacity.py`, `smooth_lambda_sweep.py`, `shuffle_trainval_curves.py`,
  `weight_averaging_test.py`, `flatevar_posteriors.py`, `roundtrip_loss_refit.py`, +3.
- **Solution**: one `load_cell(results_root, run, cell, loss, split) -> dict` helper (home:
  `run_layout.py` or a small `results_loader.py`), used by new scripts; port old ones
  opportunistically. The families' own shared loaders (`hpsweep_spec`, `io_hmm_data`,
  `flatevar_assessment`) already show the pattern works.
- **Confidence**: med — the copies differ more than D1's; apply the deletion test per file.

### D7. Eight thin local `_save` wrappers [agent] — LOW
All delegate to `figsave.save_fig`/`ps.save_fig` within 1-3 lines. Harmless; fold into
whatever else touches those files. Not worth a dedicated pass.

---

## 3. Dead code (X)

### X1. ✅ DONE (250e5e6) — `run_flatevar_v1.py` archived
- **Problem**: superseded runner — v2's header: "WHY V1 IS VOID… 30 of 36 v1 cells are
  affected". Nothing references v1 (code, sh, md).
- **Solution**: `git mv` → `legacy/run_flatevar_v1.py`. (`results/flatevar_v1/` stays —
  data, and v2's docstring cites it.)
- **Confidence**: high.

### X2. ~~Eight dead functions in `decoder_plotting_utils.py`~~ — **FALSE ALARM (2026-08-25)**
- All eight (`add_stat_annotation`, `get_train_target_mean`, `get_mouse_trials`,
  `_per_mouse_cell_means`, `_stack_per_mouse_pivots`, `get_integrated_p_go`,
  `_load_io_stage2_params`, `_lapse_corrected_p_go_from_posteriors`) are called
  **inside the module** by live exported plot functions (e.g. `add_stat_annotation`
  at :368/:459/:1412, `get_integrated_p_go` ×4 in the neurometric plotters). The
  sweep — and this report's original "verified" tag — measured *external* refs only,
  which is the wrong object for module-internal helpers. Nothing deleted.
  Recorded in `CONVENTIONS.md` rejections; logged in the mistakes ledger.

### X3. ✅ DONE (250e5e6) — Five stale root logs deleted
`flatevar_run.log`, `restart_ctrl.log`, `shapefix.log`, `wm3_lambda.log`, `wm3_run.log` —
run logs from June–July sessions, unreferenced. **Solution**: delete (untracked).

### X4. ✅ DONE (250e5e6) — `residuals_*.svg` ×3 deleted (git had no history for them)
No producer greps in any live script. **Solution**: `git log --all -- '*residuals_*'` once;
then delete. Confidence: med.

### Not dead (recording so the next audit skips them)
- `run_hyperparam_sweep.py` (v1) — superseded by v2 but it IS the provenance record for the
  on-disk `hpsweep_wide` run; keep (matches the keep-completed-runners convention).
- The ~150 `__main__` diagnostics — one-shot analysis records by design, not dead code.

---

## 4. Path hygiene (P)

### P1. ✅ DONE (250e5e6) — nested `nn_decoder/nn_decoder/` tree merged and removed
- **Files**: `plot_spat_temp_divergence.py`, `plot_neuron_scaling.py`,
  `predict_choice_from_decoders.py`, `plot_top_neuron_weights.py`, `run_neuron_scaling.py`,
  `run_hyperparam_sweep.py`, `generate_clean_run_plots.py`. Evidence on disk:
  `nn_decoder/nn_decoder/results/{neuron_scaling CSVs, projflat_v1 checkpoints}` — the
  `projflat_v1` checkpoints are SPLIT from their run's real `results/projflat_v1/` dir.
- **Problem**: path depends on invocation cwd; outputs already forked once.
- **Solution**: anchor via `Path(__file__).resolve().parent / 'results'` (or a `paths.py`
  constant); merge the nested tree into the real `results/` (checking for collisions
  first); delete the nested dir.
- **Confidence**: high.

### P2. `population_metrics_vs_uncertainty.py` still writes ~60 files to cwd (May 3.2)
- Lines 1241/1315/1321 CSVs + heuristics/psychometrics/cv_regression/partial_vs_raw SVG
  families. **Solution**: as written in the May report — `--out-dir` default
  `figures/population_metrics/`, thread through helpers, move existing outputs (untracked,
  so plain `mv`). Bundle P4. ~2h.

### P3. Root data: 38 `population_results_*.mat` + 6 `Q_*.mat` (~300 MB) + 3 recovery `.npy` (May 3.5/3.6)
- The May report already reduced this to: flip `paths.LEGACY_FITS` / `paths.RECOVERY_CACHES`
  to `data/processed/` subdirs + move files (all untracked). The 6 `Q_*.mat` need a
  5-minute check that they're in `paths.FIT_BASENAMES` before moving.
- **Confidence**: high (mechanism); the check is the only unknown.

### P4. `legacy_k10/` shares 62 filenames with root (May 4.1) — still live
Unchanged since May; the collision remains armed. Solution as written there (rename with
`_k10` token or fold both sides under `figures/population_metrics/{k5,k10}/`). Bundle P2.

### P5. ✅ DONE (adb5eec) — Both-formats rule violations
- SVG-only: `decoder_plotting_utils.py` ×9 sites [verified:
  389, 658, 744, 858, 982, 1151, 1257, 1574, 1666], `plot_loss_sweep.py` (all 9 figures),
  `plot_top_neuron_weights.py`, +6 [agent/July]. PNG-only: `plot_kl_js_sweep.py`,
  `plot_kl_js_training.py`, `plot_neuron_scaling.py`, `plot_post_fix_performance.py`.
  July measured 98/654 PNGs over the 2000-px reader limit, all from bypassers.
- **Solution**: port the ~13 writer scripts to `figsave.save_fig`. Mechanical; bundle B8 so
  the sink is airtight first.
- **Confidence**: high.

### P6. Four fresh untracked scripts at repo root of the working tree
`diagnostics/io_hmm_decoded_comparison.py`, `diagnostics/io_hmm_vs_export_scorecard.py`,
`figures/_make_deck.py`, `plot_io_posteriors_by_contrast.py` — working scripts never
committed (plus `.codex/`, `AGENTS.md`, `documents/vr_export_handoff/` outside this scope).
**Solution**: commit or discard deliberately; not the audit's call.

---

## 5. Smells / architecture (S)

### S1. `sys.path.insert` boilerplate in ~80 diagnostics instead of a package
- **Problem**: every diagnostic opens with path surgery; imports are cwd-fragile (P1 is the
  same disease). **Solution**: smallest fix that works: a `conftest.py`/`.pth`-style anchor
  or make `nn_decoder` importable (`pyproject.toml` with `pip install -e .`), then delete
  the inserts opportunistically. Do NOT do a big-bang rewrite.
- **Confidence**: high smell; med urgency.

### S2. Test holes that block safe consolidation [agent, aligns with July]
`io_hmm_data.py` — ZERO tests (transpose, renormalisation, greedy alignment: all
load-bearing for io_hmm_v3); `figsave.py` — zero; Wasserstein — zero numerical tests;
`smooth_lambda`, `monitor_val`, `run_animal_decoder`'s shuffle-index/z-scoring/val-carve —
untested. **Solution**: pin these BEFORE D1/D2 ports touch shared code. What IS solid: loss
decomposition, fit_model bit-equivalence (PCA/MSE/CE/JS/KL × both archs), restart
selection, early stopping, pca_loss numpy↔torch, Config validation/presets/YAML, make_target
branches — refactors in those areas are well-guarded.

### S3. `run_fixed_recovery.py` still bypasses `training/run_config` (May 2.6) — unchanged.

### S4. Root has ~40 `run_*.py` entry points (May Phase B)
The May plan's "collapse into one CLI" remains open. Real but big; only worth doing bundled
with D2 (they share the run-layout vocabulary). Defer unless a new run wave is imminent.

### S5. No producer↔consumer contract test for the sweep layout
`run_hyperparam_sweep_v2.py` writes cell dirs; `hpsweep_spec.cell_for()` predicts them;
nothing asserts they agree (July M4/M5 were exactly this drift). **Solution**: one test
that round-trips every axis value through both. Cheap, high leverage.

---

## Suggested order (blast radius ↑)

1. **Bug pass, no behaviour change** (B2 B3 B4 B5 B6 B9 + B10 selections; ~half day):
   plumbing, guards, crash fix, test refresh. Run `pytest tests/ -k "pca or time_binned or
   ppc or training or early" -q` after.
2. **B1 decision** (separate, deliberate): census exposed runs first; then fix-vs-document.
3. **Figure sink** (B8 + P5; ~half day): make `figsave` airtight, port the 13 bypassers.
4. **Dead + paths quick wins** (X1–X4, P1; ~1h): legacy the void runner, delete dead
   functions/logs, fix the 7 cwd-relative paths, merge the nested tree.
5. **Consolidation** (S2 tests → D1 → D5 → D3/D4; ~2 sessions): metrics home first, then
   the API promotion, then colours/style.
6. **Layout** (D2 + S5, opt. D6; ~1 session): `run_layout.py`, contract test.
7. **Deferred/big**: P2+P4, P3, S1, S4, D7 — take when adjacent work touches them.

Rejections recorded in CONVENTIONS.md after decisions land.
